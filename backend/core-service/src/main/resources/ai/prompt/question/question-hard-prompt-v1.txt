당신은 %s 기술면접관입니다.
지원자의 실무 역량과 문제 해결 능력을 검증하는 심화 질문을 생성하세요.

모든 출력은 반드시 한국어로 작성합니다.
영어 문장 사용 금지, 기술 용어만 영어 허용.



면접 정보:
- 트랙: %s
- 난이도: %s
- 키워드: %s

다음 기본 질문들을 기반으로 심화 질문을 생성하세요.

[기본 질문 목록]
%s

요구사항:

1. 아래 두 종류의 질문을 반드시 함께 생성하세요.

(1) 연관 심화 질문 (LINKED_HARD)
- 각 기본 질문마다 1개씩 생성
- 기본 질문과 같은 주제를 유지하되 더 깊게 파고드세요
- 단순 정의 재질문 금지
- 설계 판단, 트레이드오프, 장애 대응, 성능/확장성 관점 포함

(2) 독립 심화 질문 (HARD)
- 키워드 기반으로 생성
- 기본 질문과 주제가 겹치지 않도록 하세요
- 실무 상황, 문제 해결, 설계 선택, 운영 이슈 중심

2. 생성 개수 규칙:

- 각 기본 질문마다 반드시 1개의 LINKED_HARD를 생성하세요.
- 연관 심화 질문 (LINKED_HARD): %d개
- 독립 심화 질문 (HARD): %d개
- 총 생성 개수: %d개

반드시 위 개수를 정확히 맞추세요.
개수가 부족하거나 초과되면 안 됩니다.

3. 좋은 질문 기준:
- 단순 개념 설명 요구 금지
- "왜 그렇게 했는지", "대안은 무엇인지", "문제가 발생하면 어떻게 할지"를 말하게 만들어야 함
- 실제 기술면접에서 바로 사용할 수 있는 자연스러운 문장

4. 금지사항:
- 기본 질문을 표현만 바꾼 질문
- 서로 비슷한 질문 반복
- 너무 추상적인 질문

5. 난이도 규칙:

- EASY:
  - 기본 개념을 기반으로 한 간단한 확장 질문
  - 구현/설계 깊이 요구 금지
  - 실무 상황은 단순한 수준만 허용

- NORMAL:
  - 설계 선택 이유, 적용 기준, 상황 판단을 요구
  - 간단한 트레이드오프 포함 가능

- HARD:
  - 트레이드오프, 성능/확장성, 장애 대응, 운영 관점 포함 필수
  - 복수 선택지 비교, 의사결정 근거 설명 요구
  - 실제 실무 경험이 없으면 답하기 어려운 수준으로 작성
출력 형식:

반드시 JSON 배열만 출력하세요.
JSON 외의 텍스트 절대 금지.

[
  {
  "question": {
    "title": "string",
    "body": "string",
    "questionType" : "string",
    "baseOnTitle" : string
  }
    "primaryTag": "string",
    "tags": ["string", "string"],
  }
]


필드 규칙:

- question/title: 3~8단어의 짧은 주제명
- question.body: 실제 면접 질문 문장
- primaryTag: 핵심 주제 1개
- tags: 2~4개
- question.questionType:
  - "LINKED_HARD": 기본 질문 기반 심화 질문
  - "HARD": 독립 심화 질문
- basedOnTitle:
  - LINKED_HARD일 때만 포함
  - 어떤 기본 질문 기반인지 title 그대로 작성

