## 📦 수정 사항

[🔝 메인 목차로 이동](../../readme.md)

<details>
<summary> 수정 사항 (2026.03.30)</summary>

#### 📌 문제 상황
- 같은 사용자가 여러 번 면접 질문 생성 시 ai가 같은 질문을 자주 내려줌

#### 🔍 원인
- 프롬프트 입력에 제외 조건을 넣지 않아 ai가 같은 질문을 내려줌
- 
#### 🛠 해결
- 면접 질문 생성 시 primaryTag를 내려주며 면접 질문 생성 시 
  최근 30개의 질문을 조회 후 primaryTag값을 질문 생성 프롬프트에 추가 하여
  중복 질문 생성 최대한 안되게 설정을 하였습니다.

</details>

<details>
<summary> 수정 사항 (2026.03.31)</summary>

#### 📌 문제 상황
- 리프래시 토큰 내려주는 쿠키가 여러 곳에서 따로 따로 만들며 삭제 하는 로직이 있어
  관리하기가 애매했습니다.

#### 🔍 원인
- 개발 중 여러 이슈로 인해 공통 쿠키 생성을 만들었으나 사용하지 않고 각 클래스에서
  메소드를 만들어서 사용했습니다.

#### 🛠 해결
- common-spring 모듈에 customcookie 를 생성 후 customcookie를 통해 생성,삭제
  하도록 처리 하였습니다.

#### 📌 추가 사항
- stt ai api를 이용하여 사용자가 텍스트 뿐만이 아닌
  보이스로 녹음 후 텍스트로 변환 되는 로직을 추가 하였습니다.

</details>

<details>
<summary> 수정 사항 (2026.04.01)</summary>

#### 📌 문제 상황
- 꼬리 질문 생성 시 의미 없는 답변 , 짧은 답변 일 경우 무조건 꼬리 질문을 생성하는 문제가 있었습니다.
- 딥 다이브 질문 생성 시 의미 없는 답변 ,짧은 답변 애매한 답변 일 경우 딥 다이브 질문을 생성하는 문제가 있었습니다.

#### 🔍 원인
- 꼬리 질문 생성 시 서버에서 체크 [빈값 짧은 답변 , 금지어] 만 하고 있었습니다.
 그래서 빈값 , 금지어 제외 하고 의미 없는 답변 , 짧은 답변 일 경우 답변이 이상해도 꼬리질문을 생성하고 있었습니다.
- 딥다이브 질문 생성 서버에서 체크 [빈값 , 짧은 답변 , 금지어] 만 하고 있었습니다.
 그래서 빈값 , 금지어 , 짧은 답변을 제외 하고 답변이 질문과 상관 없어도 딥다이브 질문을 생성 하고 있었습니다.

#### 🛠 해결
- 꼬리 질문 생성 시 서버에서 체크 [빈값] 을 하고
  꼬리질문을 생성할 수 있는지에 대한 ai api를 호출 하고 응답 값이 true 일 경우에만
  꼬리질문 생성 ai api를 호출하여 꼬리질문을 내려 주게 수정을 하였습니다.
- 딥다이브 질문 생성 시 서버에서 체크 [빈값] 을 하고
  딥다이브 질문을 생성할 수 있는지에 대한 ai api를 호출 하고 응답 값이 true 일 경우에만
  딥다이브 질문 생성 ai api를 호출하여 딥다이브 질문을 내려 주게 수정을 하였습니다.

</details>

<details>
<summary> 수정 사항 (2026.04.06)</summary>

#### 📌 문제 상황
- 부하 테스트 진행 중 로그인 API에서 응답 시간이 평균 2초 이상으로 나타나며, 일부 요청은 p95 기준 최대 8초까지 지연되는 문제가 발생했습니다.

#### 🔍 원인
- 로그인 성공 후 auth-service에서 core-service로 전달하는 API 호출이 동기 방식으로 처리되고 있었습니다.
- 해당 호출은 로그인 핵심 로직과 무관한 부가 작업(실패 카운트 초기화, 마지막 로그인 시간 갱신)이었음에도 응답 반환 전에 실행되어 전체 latency를 증가시키는 원인이 되었습니다.
- 특히 동시 요청 증가 시 외부 API 응답 대기로 인해 스레드가 점유되며 tail latency가 크게 증가했습니다.

#### 🛠 해결
- 로그인 핵심 경로와 부가 작업을 분리하기 위해, 해당 로직을 Kafka 기반 비동기 이벤트 처리 방식으로 변경했습니다.
- 로그인 성공 시 이벤트를 발행하고, core-service에서 이를 소비하여 후처리를 수행하도록 구조를 개선했습니다.

#### 📊 개선 결과
- median: 약 2.5s → 1.46s
- p95: 약 8s → 2.48s

동기 호출 제거를 통해 응답 지연을 줄이고, tail latency를 크게 개선할 수 있었습니다.

</details>

<details>
<summary> 수정 사항 (2026.04.07)</summary>

#### 📌 문제 상황
- 부하 테스트 진행 중 동일 사용자에 대해 면접 생성 요청이 동시에 들어올 경우,
  인터뷰가 중복 생성되거나 의도와 다른 개수로 생성되는 문제가 발생했습니다.
- 특히 OpenAI 호출이 포함된 긴 처리 과정 중 추가 요청이 들어오면서 데이터 정합성이 깨질 가능성이 있었습니다.

#### 🔍 원인
- 인터뷰 생성과 질문 생성(OpenAI 호출 포함)이 하나의 흐름으로 처리되면서,
  생성 완료 전 상태(`ACTIVE`)만 기준으로 조회할 경우 동시 요청을 완전히 제어하지 못했습니다.
- 질문 생성 과정에서 외부 AI 호출 시간이 길어지는 동안, 동일 사용자 요청이 추가로 들어와
  **생성 중 상태를 인지하지 못하고 새로운 인터뷰를 생성하는 race condition**이 발생할 수 있었습니다.
- 또한 긴 트랜잭션 구조로 인해 DB 락 점유 시간이 길어지면서,
  동시성 상황에서 처리 흐름이 비효율적으로 동작했습니다.

#### 🛠 해결
- 인터뷰 상태를 `PENDING`, `GENERATING`, `ACTIVE`로 확장하고,
  생성 중 상태(`GENERATING`)까지 포함하여 재사용하도록 로직을 개선했습니다.
- `userId` 기준 비관적 락(`findByIdForUpdate`)을 적용하여
  동일 사용자 요청을 직렬화했습니다.
- 인터뷰 생성과 질문 생성 과정을 분리하여
  **트랜잭션을 짧게 유지**하도록 구조를 개선했습니다.
  - 인터뷰 생성: 짧은 트랜잭션
  - 질문 생성 준비: 짧은 트랜잭션
  - OpenAI 호출: 트랜잭션 외부 수행
  - 질문 저장: 짧은 트랜잭션
- idempotencyKey 기반 재요청 처리 로직을 통해
  동일 요청에 대한 중복 생성도 방지했습니다.
#### 📊 개선 결과
- 동일 사용자에 대한 동시 요청 상황에서도
  인터뷰가 1건만 생성되도록 보장할 수 있었습니다.
- 트랜잭션 분리를 통해 DB 락 점유 시간이 감소하여
  동시성 환경에서 안정적인 처리 흐름을 확보했습니다.

</details>

<details>
<summary> 수정 사항 (2026.04.08)</summary>

#### 📌 문제 상황
- AI 면접 평가 결과가 질문마다 일관되지 않고, 동일한 수준의 답변임에도 점수가 다르게 나오는 문제가 있었습니다.
- 특히 GPT가 생성하는 평가가 “느낌 기반”으로 이루어지면서,
  평가 기준이 명확하지 않고 사용자 입장에서 납득하기 어려운 결과가 발생했습니다.
- 면접 직무 수정 시 면접의 핵심 태그를 이전 면접에서 가져오는 문제가 있었습니다.
  그리하여 직무를 수정 하였음에도 이전 면접 직무의 질문이 나오는 현상이 있었습니다.

#### 🔍 원인
- 기존 평가 방식은 명확한 기준 없이 AI가 답변을 종합적으로 판단하는 구조였기 때문에,
  질문 유형이나 표현 방식에 따라 점수 편차가 발생할 수 있었습니다.
- 평가 기준이 정형화되어 있지 않아,
  기술적 정확성, 실무 적합성, 구체성 등의 요소가 일관되게 반영되지 않았습니다.
- 면접 질문 생성 시 최대 30개의 핵심 태그를 제외 하여 질문을 생성하는데 조회 시 직무에 상관없이 
  조회되는 현상이 있었습니다.

#### 🛠 해결
- 직무별 점수 평가 기준(rubric)을 정의하고, 질문별 평가에 적용하도록 개선했습니다.
- rubric은 문제 이해, 설계 능력, 성능 고려, 트레이드오프 등
  실무 중심 항목으로 구성하여 평가 기준을 명확히 했습니다.
- AI는 rubric을 참고하여 항목별 평가를 수행하도록 하고,
  점수 산정의 일관성을 유지하도록 프롬프트를 개선했습니다.
- 총평은 rubric을 재사용하지 않고, 질문별 평가 결과를 기반으로 종합 요약하도록 구조를 분리했습니다.
- 핵심 태그 조회 쿼리를 현재 사용자의 직무에 맞게 쿼리가 수정 되었습니다.

#### 📊 개선 결과
- 동일 수준의 답변에 대해 일관된 점수 산정이 가능해졌습니다.
- 평가 기준이 명확해져 사용자 입장에서 피드백의 신뢰도가 향상되었습니다.
- 질문 유형이 달라도 동일한 기준으로 평가되어,
  전체 면접 결과의 일관성과 안정성이 개선되었습니다.
- 면접 직무 수정 후 면접 생성 시 해당 직무에 맞는 질문이 생성 되었습니다.


</details>

<details>
<summary> 수정 사항 (2026.04.09)</summary>

#### 📌 문제 상황
- AI 면접 질문 생성 시 기본 개념 질문 없이 경험/트레이드오프 중심 질문만 생성되고 있었습니다.
- 이로 인해 질문 난이도가 과도하게 높아지고, 개념 기반 평가가 어려운 문제 발생되고 있었습니다.

#### 🔍 원인
- AI 프롬프트에 경험/트레이드오프 관련 조건만 포함되어 있었으며,
  기본 개념 질문 생성 조건이 누락되고 있었습니다.

#### 🛠 해결
##### 1. 질문 정책 수립
면접 질문 개수에 따라 기본 개념 + 심화 질문(pair)과 추가 심화 질문을 조합하는 정책을 수립을 진행하였습니다.

- 3개 → (기본 + 심화) 1세트 + 심화 1개
- 5개 → (기본 + 심화) 1세트 + 심화 3개
- 7개 → (기본 + 심화) 2세트 + 심화 3개
- 10개 → (기본 + 심화) 2세트 + 심화 6개

##### 2. 질문 선택 로직 개선
기본/심화 질문을 하나의 pair 단위로 구성
pair 선택 후 남은 질문에서 심화 질문을 추가 선택
- 다음 기준으로 가중치 적용:
  - 이전 면접 질문과 중복 시 감점
  - 동일 primaryTag 반복 시 감점

#### 📊 개선 결과
- 기본 개념 → 심화 질문으로 이어지는 구조 확보 되었습니다.
- 특정 주제 편중 감소 및 질문 다양성 증가가 되었습니다.
- 면접 질문 난이도 분포가 안정적으로 개선되었습니다.


#### 📌 문제 상황
- 면접 질문 생성 시 원래 의도는 직무 관련만 추가하여 면접 질문을 생성하고 있었습니다.
- 그리하여 직무 관련 범위가 너무 넓어 포괄적인 느낌의 면접 질문만을 생성하고 있었습니다.

#### 🔍 원인
- 면접 질문 생성 시 직무 관련만 입력되어 포괄적인 면접 질문을 생성하고 있었습니다.

#### 🛠 해결
- 면접 세팅 관련 로직에 면접 키워드를 입력되게 하여 면접 키워드 관련된 부분의 면접 질문만 생성하게 수정하였습니다.

#### 📊 개선 결과
- 포괄적인 면접 질문이 아닌 사용자가 선택한 면접 키워드 중에서 랜덤으로 면접 질문이 생성 되고 있습니다.

</details>