pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        mavenCentral()
    }
}

rootProject.name = "mockio"

include "gateway"
project(":gateway").projectDir = file("backend/gateway")

include "auth-service"
project(":auth-service").projectDir = file("backend/auth-service")

include "core-service"
project(":core-service").projectDir = file("backend/core-service")

include "support-service"
project(":support-service").projectDir = file("backend/support-service")

include "common-core"
project(":common-core").projectDir = file("backend/common-core")

include "common-spring"
project(":common-spring").projectDir = file("backend/common-spring")

include "common-jpa"
project(":common-jpa").projectDir = file("backend/common-jpa")

include "common-security"
project(":common-security").projectDir = file("backend/common-security")

include "common-kafka"
project(":common-kafka").projectDir = file("backend/common-kafka")

include "common-ai-contractor"
project(":common-ai-contractor").projectDir = file("backend/common-ai-contractor")

